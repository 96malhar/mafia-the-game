# Taskfile for the Mafia game.
# https://taskfile.dev
#
# Install task: `brew install go-task` (macOS) or see https://taskfile.dev/installation
# Run `task --list` to see available tasks.

version: "3"

vars:
  BIN_DIR: bin
  BINARY: "{{.BIN_DIR}}/server"
  PKG: ./...
  MAIN: ./cmd/server
  COVER_FILE: coverage.out
  IMAGE: mafia-the-game

env:
  CGO_ENABLED: "1" # required for -race

tasks:
  run:
    desc: Start the dev server (ADDR=:8080 by default).
    cmds:
      - go run {{.MAIN}}

  sim:
    desc: Run a scripted bot game against a running server. Start `task run` in another shell first.
    # Bots open WebSockets to ${ADDR:-:8080}, so the same env var that
    # `run` and `stop` honour applies here too.
    cmds:
      - go run ./cmd/sim -addr "http://localhost${ADDR:-:8080}" {{.CLI_ARGS}}

  lobby:
    desc: Open 8 Chrome tabs that auto-join a fresh room, waiting to start. Start `task run` first. (macOS)
    # Creates one empty room via the API, then opens a tab per player at
    # /?room=CODE&name=NAME. The web client auto-joins when a link carries
    # both params (see maybeAutoJoinFromURL in web/index.html). The FIRST
    # tab to connect becomes host (first-joiner-is-host), so it gets the
    # "Start game" controls; the rest sit in the lobby. Uses macOS `open`.
    cmds:
      - |
        base="http://localhost${ADDR:-:8080}"
        code=$(curl -fsS -X POST "$base/api/rooms" | sed -n 's/.*"code":"\([^"]*\)".*/\1/p')
        if [ -z "$code" ]; then
          echo "could not create room — is 'task run' up on ${ADDR:-:8080}?" >&2
          exit 1
        fi
        echo "room $code — opening 8 Chrome tabs (host: Alice)"
        # Tabs open in Chrome specifically: it throttles background-tab
        # audio far less than Safari, so the host tab keeps narrating even
        # while you click around the other players' tabs in the sim.
        #
        # Open the host tab FIRST and give it a real head start. The host
        # is whoever's join lands first server-side, not whoever's `open`
        # runs first — a cold tab needs time to load the page, open its
        # WebSocket, and send the join. Too short a gap and a later tab
        # wins the race, so the host ends up arbitrary. ~2.5s reliably
        # lets Alice connect + claim host before the rest pile in.
        open -a "Google Chrome" "$base/?room=$code&name=Alice"
        sleep 2.5
        for name in Bob Charlie Diana Edward Fiona George Hannah; do
          open -a "Google Chrome" "$base/?room=$code&name=$name"
          sleep 0.3
        done

  stop:
    desc: Stop the dev server (ADDR=:8080 by default). Sends SIGTERM, then SIGKILL after 5s.
    # We target the listening socket rather than a process name so this
    # works whether the server was started via `task run` or
    # `./bin/server`. Respects $ADDR like `run` does.
    cmds:
      - |
        addr="${ADDR:-:8080}"
        port="${addr#*:}"
        pids=$(lsof -ti tcp:"$port" -sTCP:LISTEN 2>/dev/null || true)
        if [ -z "$pids" ]; then
          echo "no process listening on port $port"
          exit 0
        fi
        echo "stopping server on port $port (pids: $pids)"
        kill $pids 2>/dev/null || true
        # Wait up to 5s for graceful shutdown; main.go has its own 10s
        # window but we don't want to block the dev loop that long.
        for i in 1 2 3 4 5; do
          sleep 1
          still=$(lsof -ti tcp:"$port" -sTCP:LISTEN 2>/dev/null || true)
          [ -z "$still" ] && exit 0
        done
        echo "graceful stop timed out; sending SIGKILL"
        kill -9 $still 2>/dev/null || true

  build:
    desc: Build a release binary to bin/server.
    cmds:
      - mkdir -p {{.BIN_DIR}}
      - go build -trimpath -ldflags="-s -w" -o {{.BINARY}} {{.MAIN}}
    sources:
      - "**/*.go"
      - go.mod
      - go.sum
    generates:
      - "{{.BINARY}}"

  docker:build:
    desc: Build the production Docker image (tag with TAG=..., default 'latest').
    # Stamp main.version from git so local images report a real version
    # (falls back to the short SHA, or "dev" outside a git checkout).
    vars:
      VERSION:
        sh: git describe --tags --always --dirty 2>/dev/null || echo dev
    cmds:
      - docker build --build-arg VERSION={{.VERSION}} -t {{.IMAGE}}:{{.TAG | default "latest"}} .

  docker:smoke:
    desc: Build the image, boot it, and verify GET /healthz responds (CI runs this too).
    # Single source of truth for the container smoke test so local runs and
    # CI exercise the exact same steps. Override host port with PORT=...
    deps:
      - docker:build
    vars:
      NAME: mafia-smoke
      PORT: '{{.PORT | default "8080"}}'
    cmds:
      # Tear the container down even if the health probe fails.
      - defer: docker rm -f {{.NAME}} >/dev/null 2>&1 || true
      - docker run -d --name {{.NAME}} -p {{.PORT}}:8080 {{.IMAGE}}:{{.TAG | default "latest"}}
      - |
        for i in $(seq 1 30); do
          if curl -fsS "http://localhost:{{.PORT}}/healthz"; then
            echo " -> healthz OK"
            exit 0
          fi
          sleep 1
        done
        echo "healthz did not become ready in time" >&2
        docker logs {{.NAME}} || true
        exit 1

  docker:run:
    desc: Run the production image, publishing PORT (default 8080) to the container.
    # Depends on docker:build so the image is fresh. Override the host
    # port with PORT and pass extra `docker run` flags via CLI_ARGS, e.g.
    # `task docker:run PORT=9000 -- -e LOG_LEVEL=debug`.
    deps:
      - docker:build
    cmds:
      - docker run --rm -p {{.PORT | default "8080"}}:8080 {{.CLI_ARGS}} {{.IMAGE}}:{{.TAG | default "latest"}}

  test:
    desc: Run all tests with the race detector.
    cmds:
      - go test -race -count=1 {{.PKG}}

  test:cover:
    desc: Run tests and open an HTML coverage report.
    cmds:
      - go test -race -coverprofile={{.COVER_FILE}} {{.PKG}}
      - go tool cover -html={{.COVER_FILE}}

  bench:
    desc: Run benchmarks.
    cmds:
      - go test -bench=. -benchmem -run=^$ {{.PKG}}

  tidy:
    desc: Sync go.mod / go.sum.
    cmds:
      - go mod tidy

  fmt:
    desc: Format Go source. Uses goimports if available, otherwise gofmt.
    cmds:
      - |
        if command -v goimports >/dev/null 2>&1; then
          goimports -w -local github.com/96malhar/mafia-the-game .
        else
          gofmt -s -w .
        fi

  vet:
    desc: Run `go vet`.
    cmds:
      - go vet {{.PKG}}

  lint:
    desc: Run golangci-lint (install with `task tools` if missing).
    cmds:
      - |
        if ! command -v golangci-lint >/dev/null 2>&1; then
          echo "golangci-lint not found. Run 'task tools' to install it."
          exit 1
        fi
        golangci-lint run

  vuln:
    desc: Scan dependencies for known vulnerabilities via govulncheck.
    # Requires network access to fetch the vuln DB from vuln.go.dev.
    # Kept out of `task check` so the fast local loop stays offline-
    # friendly; included in `task ci` for merge-gate scans.
    cmds:
      - |
        if ! command -v govulncheck >/dev/null 2>&1; then
          echo "govulncheck not found. Run 'task tools' to install it."
          exit 1
        fi
        govulncheck ./...

  check:
    desc: Fast pre-commit gate — fmt, vet, lint, test. Offline-friendly.
    cmds:
      - task: fmt
      - task: vet
      - task: lint
      - task: test

  ci:
    desc: Full CI pipeline — everything in `check` plus a vulnerability scan.
    cmds:
      - task: check
      - task: vuln

  tools:
    desc: Install/refresh dev tools (goimports, golangci-lint, govulncheck).
    cmds:
      - go install golang.org/x/tools/cmd/goimports@latest
      - go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest
      - go install golang.org/x/vuln/cmd/govulncheck@latest

  clean:
    desc: Remove build and coverage artifacts.
    cmds:
      - rm -rf {{.BIN_DIR}} {{.COVER_FILE}}
